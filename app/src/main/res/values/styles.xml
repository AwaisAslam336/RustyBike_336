<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="scroll_style">
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:scrollbars">vertical</item>
        <item name="android:fadeScrollbars">false</item>
        <item name="android:scrollbarSize">10dp</item>
        <item name="android:scrollbarThumbVertical">@drawable/scroll_icon_resize</item>
        <item name="android:scrollbarTrackVertical">@drawable/scrollbar_bg</item>
    </style>
    <style name="spinner_style">
        <item name="android:background">@drawable/app_spinner</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#999da2</item>
    </style>
</resources>
